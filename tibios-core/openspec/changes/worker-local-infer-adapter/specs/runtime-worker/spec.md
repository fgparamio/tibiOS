No changes land in `runtime-worker` for this change. Zero requirements here.

`sdd-design` settled the proposal's open D0 placement decision as **D0-b**: the engine port, the reference engine, `LocalInferWorker`, its blocking boundary, and `AnyWorker` all live inside the Composition Root at `runtime/src/worker/local_infer/` (see `design.md` D4-D11). `runtime-worker` gains no new module, no new trait, no new public item, and no new machine-checked invariant.

The one file this change touches under `crates/runtime-worker/` is `src/ports/worker_service.rs`'s `AnyWorker` doc-comment sketch (design D10): the existing sketch illustrates `Either`-based lazy dispatch, which — read literally by a future implementer — produces an `async move` wrapper that defers each inner Worker's O1 registration to first poll, silently breaking the obligation. The sketch needs correcting to the eager-match-then-`Box::pin` shape, with the O1 hazard documented in the comment. This is a doc-comment-only edit with no accompanying trait, type, or behavior change, and design's own File Changes table lists it as its own row, separate from the spec-delta row it hands to `sdd-spec` — so it is carried here as a **task-level implementation note**, not a new spec requirement. `worker-inbound-port/spec.md` does not currently treat that doc sketch's content as normative (it never mentions `AnyWorker`'s illustrative code at all), and the sketch's *behavior* is already fully covered, normatively, by `runtime-composition-root`'s `AnyWorker` eager-dispatch requirement plus the shared O1-O4 harness's third invocation (against `AnyWorker`) — see those specs. Fixing the comment's wording changes no observable behavior, so it earns no separate scenario.

Earlier drafts of this file (written against the proposal's original two designs — first a new `local-infer` crate, then a `runtime-worker`-internal module) are superseded in full. See `worker-local-infer-adapter/spec.md` for the real, current spec.

//! Stub for the Deployment domain.
//!
//! Implements `29-deployment.md`.
//!
//! `runtime-deployment` depends on `runtime-primitives` only. It is wired to
//! every other domain exclusively by the Composition Root (`runtime`); it
//! must never depend on another domain crate directly.
